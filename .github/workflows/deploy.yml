# Vercel auto-deploys on push via native GitHub integration.
# This workflow is kept as a no-op placeholder.
# All changes go through PRs — Nissan merges to main to trigger production deploy.
name: Deploy (via Vercel GitHub Integration)

on:
  push:
    branches:
      - main

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Vercel deploys automatically via GitHub integration on merge to main."
